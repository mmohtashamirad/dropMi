{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug dropMi server",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/backend/dropMi",
      "cwd": "${workspaceFolder}",
      "args": [
        "-config",
        "${workspaceFolder}/tmp/data/config/dropMi.cfg"
      ]
    }
  ]
}